# Release v0.1.0

## 手记视图插件 - 初始版本

### 功能特性

✅ **核心功能**
- 将 Markdown 文件按日历组织显示
- 支持从正文中提取图片（`![[image]]` 和 `![](path)` 格式）
- 按月份分组显示手记条目
- 卡片式布局，类似手记应用

✅ **性能优化**
- 分页加载（每页 20 个条目）
- 图片懒加载
- 使用 Obsidian Metadata Cache 优化
- 增量渲染

✅ **用户体验**
- 现代化的欢迎界面（应用 UI/UX Pro Max 设计原则）
- 右键菜单快速创建文件夹手记视图
- 支持文件夹和子文件两种手记视图模式
- 在主内容区域显示视图

✅ **UI/UX 优化**
- SVG 图标（非 emoji）
- 平滑过渡动画（150-300ms）
- 无障碍支持（焦点状态、aria-label）
- 响应式设计

### 安装文件

用户只需要以下 4 个文件：
- `manifest.json`
- `main.js`
- `styles.css`
- `data.json`（可选，首次运行自动创建）

### 使用方法

1. **手动触发扫描**
   - 打开手记视图后，点击"开始扫描"按钮

2. **创建文件夹手记视图**
   - 右键文件夹 → 选择"手记" → "创建文件夹手记视图"
   - 之后点击该文件夹会自动打开手记视图

3. **创建子文件手记视图**
   - 右键文件夹 → 选择"手记" → "创建子文件手记视图"
   - 会在文件夹下创建一个手记视图文件
   - 点击该文件会自动打开手记视图

### 技术细节

- **Obsidian 版本要求**: >= 0.15.0
- **TypeScript**: 4.7.4
- **构建工具**: esbuild 0.17.3

### 已知问题

- 首次加载大量文件时可能需要几秒钟
- 图片路径解析依赖 Obsidian 的 metadata cache

### 更新日志

#### v0.1.0 (2026-02-15)
- 🎉 初始版本发布
- ✨ 基础手记视图功能
- ✨ 图片提取和显示
- ✨ 右键菜单集成
- ✨ 性能优化
- ✨ UI/UX 优化

---

## 如何在 GitHub 创建 Release

1. 访问仓库：https://github.com/Lam-L/ObJournal
2. 点击右侧 "Releases" → "Create a new release"
3. 选择标签：`v0.1.0`
4. 填写 Release 标题：`v0.1.0 - 初始版本`
5. 复制上面的内容作为 Release 说明
6. 上传以下文件作为附件（可选）：
   - `manifest.json`
   - `main.js`
   - `styles.css`
7. 点击 "Publish release"
